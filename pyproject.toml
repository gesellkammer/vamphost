[build-system]

requires = [
    "setuptools>=42",
    "wheel",
    "numpy>=1.22"
]

build-backend = "setuptools.build_meta"


[tool.cibuildwheel]
build = "cp38-* cp39-* cp310-*"

skip = "pp* *686* *-musllinux_*"
